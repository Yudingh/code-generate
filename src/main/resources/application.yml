spring:
  application:
    name: ai-code-genrate
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_code_generate
    username: root
    password: Qwer15896353225
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ydh.aicodegenrate.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
# 替换成你自己的 mapper 包路径
logging:
  level:
    com.ydh.aicodegenrate.mapper: debug